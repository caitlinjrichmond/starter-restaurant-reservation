import React from "react";
import { useEffect } from "react";
import { getResByDate, listReservations } from "../utils/api"
// import { loadDashboard } from "./Dashboard"

function ResByDate() {
    // useEffect(loadDashboard, [date]);
    return (
        <div><p>I'll be a reservation for a specific date!</p></div>
    )
}

export default ResByDate